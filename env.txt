# Copy the matching section into each service's Environment settings.
# This platform does not load .env files directly.

[frontend]
HOST=0.0.0.0
NODE_ENV=development
PORT=5173
VITE_BASE_URL=${domainUrl::backend::3000}

[backend]
HOST=0.0.0.0
NODE_ENV=development
PORT=3000
DATABASE_URL=postgres://${env::database::POSTGRES_USER}:${env::database::POSTGRES_PASSWORD}@${host::database}:${env::database::POSTGRES_PORT}/${env::database::POSTGRES_DB}
CORS_ORIGIN=${domainUrl::frontend::5173}
JWT_ACCESS_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret

[database]
DB_TYPE=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app_db
POSTGRES_PORT=5432
